# VALIDATION.md

This file defines the minimum validation required before work is considered complete in `go-specs`.

---

## Required Validation

Unless the task explicitly says otherwise, run:

```bash
go test ./...
go test -race ./...
```
