// Package dsl provides the DSL layer: Describe, It, BeforeEach, AfterEach, Spec, SuiteTree,
// Focus, Skip, BuildProgram, BuildSuite, Analyze, and path-based specs (Paths, PathBuilder, PathSpec).
// It depends on specs/compiler, specs/ctx, specs/property, and report. It does not import the root specs package.
package dsl
